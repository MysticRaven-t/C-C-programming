#include<bits/stdc++.h>
#include<string>
using namespace std;
string s;
int main()
{
	while(getline(cin,s))
	{
		if(cin.eof())
			break; 
		int len=s.length();
		cout<<len<<endl;
	}
	return 0;
}
